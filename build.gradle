allprojects {
  apply plugin: 'java'

  repositories {
    mavenCentral()
  }

  dependencies {
    testImplementation 'junit:junit:4.13.2'
  }
}

version ='1.0.0-SNAPSHOT'

project(':core') {}

project(':ports') {
  dependencies {
    implementation project(':core')
  }
}